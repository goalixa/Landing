# Staging values for goalixa-landing
# Override default values for staging environment

image:
  registry: harbor.goalixa.com
  repository: landing-staging/landing
  tag: "latest"

replicaCount: 1

namespace:
  name: goalixa-landing-staging
