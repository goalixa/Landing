# Goalixa Landing Page

This is the static landing page for the Goalixa project.

## Features

- Basic landing page with responsive layout
- Styled with simple CSS
- Served via Nginx in Docker

## How to Run.

Build the Docker image:

```bash
docker build -t goalixa-landing .
