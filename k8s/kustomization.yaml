apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: ghcr.io/goalixa/landing
    newName: harbor.goalixa.com/goalixa-landing/landing
    newTag: latest  # Change this to specific commit SHA for each deployment
