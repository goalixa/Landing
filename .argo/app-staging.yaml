apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: goalixa-landing-staging
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app: goalixa-landing
    app.kubernetes.io/part-of: goalixa
    environment: staging
spec:
  project: default
  source:
    repoURL: https://github.com/goalixa/goalixa-landing.git
    targetRevision: staging
    path: helm
    helm:
      valueFiles:
        - values-staging.yaml
      parameters:
        - name: image.tag
          value: latest  # Default, overridden by workflow
  destination:
    server: https://kubernetes.default.svc
    namespace: goalixa-landing-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
